# Single environment for BOTH generation and selection.
# Install the matched CUDA 11.3 builds of torch + torchvision first:
#   pip install torch==1.12.1+cu113 torchvision==0.13.1+cu113 \
#     --extra-index-url https://download.pytorch.org/whl/cu113
# The StyleGAN2-ADA generator compiles custom CUDA ops at first run, so it also
# needs CUDA toolkit 11.3 (CUDA_HOME=/usr/local/cuda-11.3) + gcc/g++ 8 + ninja.
# Verified with Python 3.9 + torch 1.12.1+cu113 on an RTX 3090.

torch==1.12.1
torchvision==0.13.1
numpy==1.26.3
Pillow>=9.0
click>=8.0
requests>=2.28
tqdm>=4.60
psutil>=5.9
scipy>=1.9
ninja>=1.11

# prune085 (TopPR) selection only — harmless for psi10 / psi20:
clean-fid>=0.1.35
scikit-learn>=1.1
