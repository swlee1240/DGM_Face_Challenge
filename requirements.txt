# Python packages for INFERENCE (generating the submissions).
# NOTE: the StyleGAN2-ADA generator builds two custom CUDA ops (bias_act,
# upfirdn2d) at first run. That needs a matching system toolchain:
#   - CUDA toolkit 11.3  (CUDA_HOME=/usr/local/cuda-11.3)
#   - gcc/g++ 8.x  (CUDA 11.3 does not support newer gcc)
#   - ninja (below) to compile
# These were verified with Python 3.9 + torch 1.12.1+cu113 on an RTX 3090.

torch==1.12.1          # +cu113 build:
                       #   pip install torch==1.12.1+cu113 \
                       #     --extra-index-url https://download.pytorch.org/whl/cu113
numpy==1.26.3
Pillow>=9.0
click>=8.0
requests>=2.28
tqdm>=4.60
psutil>=5.9
scipy>=1.9
ninja>=1.11            # compiles the custom CUDA ops
